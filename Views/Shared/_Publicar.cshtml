@{ 
    var Text = null as dynamic;
    var IsReply = null as dynamic;
}

<div class="row">
        
    <div class="card card-columns bg-info izqtCol">
        <img class="card-img picP" src="~/pic.png" />
    </div>

    <div class="card bg-info col-auto dertCol">
        <form asp-controller="Home" asp-action="CreateTweet">
            <div class="form-group">    
                <div class="card-body row rowtBody">
                    <textarea asp-for="@Text" class="textPubli form-control"></textarea>
                    <span asp-validation-for="@Text" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <input asp-for="@IsReply" type="hidden" value="0" />
            </div>
            <div class="card-footer row rowtFoot">
                <div class="coltFoot1">

                </div>
                <div class="coltFoot2">
                    <button type="submit" class="tSend btn btn-primary">Publicar</button>
                </div>
            </div>
        </form>
    </div>

</div>